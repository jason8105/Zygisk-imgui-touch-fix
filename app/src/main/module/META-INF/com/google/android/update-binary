#!/sbin/sh
umask 022
OIFS=$IFS
IFS=$'\n'

ZIPFILE="$2"
O31="$3"

unzip -o "$ZIPFILE" 'module.prop' -d "$O31" >/dev/null 2>&1
unzip -o "$ZIPFILE" 'customize.sh' -d "$O31" >/dev/null 2>&1

. "$O31/customize.sh"
